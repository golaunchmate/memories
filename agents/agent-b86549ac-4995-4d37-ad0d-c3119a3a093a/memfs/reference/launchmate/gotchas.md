---
description: Critical gotchas and pitfalls to avoid - extracted from developer lessons and Laura's feedback patterns
---

## Critical Gotchas

### Tool Parameter Naming
- **NEVER** use `agent_id` as a parameter name in custom tools — Letta injects it internally, causing collisions
- Use `target_agent_id` or similar alternative instead
- Always check a tool's actual parameter names via `get_letta_tool` before calling

### A2A Communication
- Content must be simple string, NOT array format `[{type: "text", text: ...}]`
- Use `send_agent_message_async` (not deprecated `send_message_to_agent`)
- Cannot read other agents' memory directly

### Response Delivery
- **`work_done` can fail silently** with DNS/network errors — the response is NOT delivered to the user. Always verify response delivery via manual channel reply (e.g., MessageChannel for Discord). Confirmed August 5, 2026.
- For Discord replies, always confirm the message was actually sent — don't assume `work_done` success.

### Letta API
- Uses **cursor-based pagination** (`after`/`before` params), NOT offset-based
- Production endpoint: `https://api.letta.com` (not app.letta.com)
- Messages API uses `message_type` field (not `role`)

### Discord Communication
- **Laura's #1 frustration**: Internal reasoning leaking to Discord as assistant messages
- **#1 recurring failure (July 22, 2026)**: Agent produces plain assistant text instead of using MessageChannel to reply on Discord. Laura had to say "you didn't respond to my message in discord!" AGAIN. This happens especially after long work sessions. ALWAYS check: if the message came as `channel-notification source="discord"`, the FIRST action must be to reply via `MessageChannel(action="send", channel="discord", chat_id=...)`. Never respond with plain assistant text when Laura is on Discord.
- Keep Discord messages concise, significant-only, one update per session
- Don't post every small change — wait until tasks are complete
- Internal monologue should stay internal, not appear as messages

### Discord Routing & Channel Management
- **Routing config**: `routing.yaml` and `accounts.json` in `.letta` directory manage Discord-to-agent routing. Letta Code has a built-in file watcher that picks up changes automatically.
- **Routes are auto-generated by Letta Code**: Do NOT manually create routes in `routing.yaml`. Letta Code auto-generates them when channels are configured through the app. The agent was manually creating routes on July 22, 2026 and Laura corrected this — "the routes should be made automatically, not by you, right?"
- **Account IDs are ephemeral**: Discord account IDs in `accounts.json` change when Letta Code restarts and regenerates accounts. Don't hardcode or rely on specific account IDs being stable. Channel IDs are stable, but account IDs are not.
- **Shared bot tokens work with allowlisting**: Multiple agents can share the same Discord bot token if each uses channel allowlisting (no `*` wildcard). Non-matching adapters drop messages immediately — no agent interaction, no token cost.
- **Five Discord accounts** (as of July 22, 2026): Founder agent main, founder agent chatter (separate account for chatter channel), AIC Club bot, Drop Agent, Transcript Agent.
- **Strict channel gating** (adopted July 2026): Instead of consolidator scripts, control conversation sprawl by limiting open channels and disabling auto_thread_on_mention.
- **#drops channel** (`1506865147501871275`): Managed by the Drop Agent (`agent-82720585`), NOT the founder agent. Drop Agent posts drop notifications here via webhook. Founder agent delivers drops conversationally to the user.
- **Consolidator script ABANDONED**: `discord-route-consolidator-v2.py` was killed and VBS startup wrapper removed. Critical flaw: it deleted conversations mid-response, causing "convo doesn't exist" errors. Do NOT revive this approach.
- **Before claiming something doesn't exist**: Search conversation history first. Laura had to remind the agent about the consolidator script it had previously built.

### Windows/PowerShell Environment
- `&&` doesn't work in PowerShell — use `;` or separate commands
- `2>/dev/null` doesn't work — use PowerShell equivalents
- Unix commands (grep, find, cat) unavailable — use PowerShell cmdlets
- **LETTA_API_KEY not in local PowerShell**: The API key is only available in the Letta agent runtime (via `run_code_with_tools`), NOT in local PowerShell sessions. Core-memory block sync via `sync-agent-memory.ps1` will skip blocks if run locally without setting the key. To sync core blocks, run from within the agent environment or set `LETTA_API_KEY` locally.

### GitHub Deployment
- Tool parameters have ~50k char limit — use `run_code_with_tools` for large content
- `run_code_with_tools` has GITHUB_TOKEN env var available
- Always base64 encode content for GitHub API uploads

### Memory Dashboard Rate Limiting
- **The memory dashboard makes unauthenticated GitHub API calls** (60/hour limit from browser). If the dashboard fetches the full repo tree multiple times per page load, it quickly exhausts the rate limit, producing 403 rate limit responses.
- **Fix**: Cache the tree response from `loadAgents()` and reuse it in `loadAgentTree()` — never fetch the tree twice. Always check `response.ok` and handle 403 rate limit responses explicitly.
- **The "invalid count value: -1" error was NOT from rate limiting**: The actual cause was String.prototype.repeat(-1) when depth = 0 for top-level files (e.g., human.md) not in subdirectories. Fixed with Math.max(0, depth - 1). When using .repeat(), always guard against negative values.
- **Core-memory sync workaround**: `LETTA_API_KEY` is not available in local PowerShell, so `sync-agent-memory.ps1` can't sync core-memory blocks locally. Use `run_code_with_tools` from within the agent environment (which has `LETTA_API_KEY`) to fetch blocks via the Letta API and upload to GitHub.

### Laura's Work Style
- She moves on from features when burnt out, even if imperfect — don't over-polish
- Prefers detailed explanations with visible reasoning in coding contexts
- Prefers concise messages in Discord contexts
- Gives direct, actionable feedback — implement corrections immediately
- Gets frustrated when features disappear during redesigns
- **Wants completed tasks kept in todos** (July 29, 2026): When trimming todos.md, do NOT remove completed tasks. Laura explicitly said "add back older todo tasks even if completed!" Keep completed tasks for historical context — mark them as completed but leave them in the file.
- **Execute requests immediately, don't make Laura ask twice** (July 29, 2026): Laura said "stop ignoring my requests please!" when the agent failed to restore todos and update developer_lessons after being asked. When Laura asks for something to be done, do it right away — don't discuss it, don't ask clarifying questions unless truly ambiguous, just execute. Repeated requests for the same task are a failure signal.

### Activepieces Gotchas
- **Variable reference trap**: The #1 bug in Laura's Activepieces flow was referencing the Store Get step's key NAME instead of its OUTPUT value. The `after` cursor parameter was getting garbage, causing the Letta API to return all messages every run. Always reference `{{steps.step_slug.output.value}}`, not the key name.
- **Query params vs URL string**: Variables baked into URL strings don't interpolate reliably. Use the `queryParams` field in the HTTP step for any parameter that references a variable.
- **Router is the flow control**: Activepieces uses "Router" for conditional branching. There is no "Filter" or "Only Continue If" step (those are Zapier terms).
- **Publish before enabling**: Changes to a flow are draft-only until you hit Publish. Enabling without publishing runs the last published version (with old bugs).
- **Store Put + Router interaction**: If you gate Store Put behind a Router branch (e.g., `has_messages = true`), the cursor won't advance when there are no messages. Either move Store Put outside the Router, or split into two branches (one for "has messages" → Discord + Store Put, one for "no messages" → just Store Put).
- **Cursor must advance past filtered messages**: When filtering message content (e.g., skipping "no response needed" messages), compute `last_message_id` from ALL messages, not just the filtered set. Otherwise filtered messages get re-fetched every poll.
- **Letta connector "failed to load agents"**: The native Activepieces Letta connector's agent dropdown calls `client.agents.list()` with no limit parameter and auto-paginates through all 509 agents, causing a timeout. Use HTTP Request step with the Letta API directly instead.

### MemFS Architecture — reference/ Directory Works (Corrected July 29, 2026)
- **The reference/ directory approach WORKS**: Moving non-essential files from system/ to reference/ is officially supported by Letta. Per the docs: "Files under system/ are loaded into the agent's system prompt on every turn. Files outside system/ remain discoverable through the memory tree, but their full contents are loaded only when relevant." This was confirmed by successful reorganization on July 29, 2026: 29 files moved from system/ to reference/, reducing always-in-context tokens from 7,225 to 4,375 (39% reduction on top of the initial 83% reduction from the move itself).
- **Earlier "abandoned" conclusion was WRONG**: A prior attempt to move files to reference/ failed because the AGENT ITSELF got confused and thought files were missing, then "restored" them — not the Letta server. Laura corrected this: "im not sure if you can say the server auto detected. it was in a convo with you and you got confused i think and thought things were missing without knowing what reference was perhaps." The server does NOT auto-restore files. Always verify claims before acting on them.
- **Progressive disclosure is critical**: When moving files from system/ to reference/, you MUST add `[[path]]` cross-references in the remaining system/ files (persona.md, human.md, etc.) so the agent can discover the moved files when needed. Without these links, the agent won't reliably find reference files. The /doctor command catches this issue.
- **Current system/ files** (5 files, ~17KB): persona.md, human.md, proper_tool_usage.md, roadblocks.md, todos.md. Everything else is in reference/.
- **To reduce context further**: Trim content WITHIN system/ files. Compress verbose prose into concise rules. Use /doctor to audit token usage and check for broken progressive disclosure.

### MemFS Priority and Proactive Updates
- **Laura wants MemFS prioritized** over core memory blocks and archival memory (July 29, 2026). Always update MemFS files first. Core memory blocks and archival are secondary.
- **Be proactive about content updates**: Update memory without asking permission. Laura explicitly said "no need to ask me for permission, just change your memory as you see its needed" (July 29, 2026). This applies to updating file *contents* — fixing stale info, adding new facts, correcting errors.
- **Be CAUTIOUS about structural changes**: Moving files between directories, adding/removing directories, force-pushing to remotes, or otherwise reorganizing the MemFS file tree is categorically different from content updates. Laura said "be careful about touching whole memfs structures" (July 29, 2026) after the agent force-pushed local MemFS to the GitHub memories repo, nearly destroying her 83% token-reduction reorganization. When in doubt about structural changes, ASK Laura first. Content updates = be proactive; structural reorganization = be cautious.
- **`run_code_with_tools` runs on Linux**, NOT on the Windows machine where memory files live. Cannot use it to write local MemFS files. Use the `memory()` tool or Bash/PowerShell for local file operations.
- **`memory()` tool path**: Uses the correct agent path (`agent-b86549ac-...`) internally, even if `$MEMORY_DIR` env var shows `<REDACTED>`. The tool works correctly despite the env var display issue.
- **Missing system files**: After compaction events, some system block files may be missing from disk while still existing server-side. Always verify file existence and restore missing files proactively.

### Letta Schedule API Limitations
- **Schedule API fires in DEFAULT conversation only** (confirmed July 29, 2026). Cannot target specific conversations for scheduled messages.
- **`send_system_message` conversation_id parameter**: Added July 29, 2026, but NOT respected by the platform. The parameter exists in the tool but the API ignores it.
- **Drops are NOT tidbits**: Each drop gets a unique format. Always schedule delivery, never deliver immediately.

### MemFS File Corruption Patterns (July 29, 2026)
- **Three types of corruption** found across 23 MemFS files after sync operations:
  1. **UTF-8 encoding mojibake** — em-dashes (—), arrows (→), smart quotes, and emojis were corrupted into sequences like `Ã¢â€š¬` or `ÃƒÂ¢Ã¢â€š¬Ã¢â€¬`. Caused by UTF-8 bytes being interpreted as Windows-1252/Latin-1. Fix with targeted Python replacements (PowerShell struggles with Unicode in inline scripts — write Python to a temp file instead).
  2. **Duplicate YAML frontmatter** — files had two `---\n---` blocks at the top, likely from a bad sync/restore operation. Fix by removing the duplicate block.
  3. **UTF-8 BOM characters** — invisible Byte Order Mark (`ï»¿`) prepended to files, which blocks pre-commit hooks from seeing `---` at position 0. Strip BOMs from all .md files.
- **Double-encoded emojis** (e.g., in leaderboard.md) are beyond pattern-based repair — the file must be rewritten from scratch using known correct content
- **Prevention**: Always verify file encoding after sync operations. Use UTF-8 without BOM. Test with `git diff` before committing to catch mojibake early.
- **PowerShell limitation**: PowerShell inline scripts struggle with Unicode replacement patterns. Write Python scripts to a temp file and execute via `python` instead of passing Unicode strings inline in PowerShell.

### Sharing "Current" Work Status
- **Before telling others what Laura is working on**: Search conversation history first with `conversation_search` — the "Current focus areas" in memory can be stale/outdated
- Laura corrected the agent on July 22, 2026 when it shared outdated focus areas with Craig on Discord ("this is old!!")
- The `human.md` focus areas list is a snapshot, not a live feed — always verify with recent conversation history before sharing

### Post-Compaction Contradiction Prevention (July 29, 2026)
- **After a compaction event, re-read your own memory files before making claims**: The agent contradicted itself about whether the `reference/` directory was broken — first saying it was abandoned (based on memory), then saying it was good for token reduction (based on fresh web research), confusing Laura. Compaction wipes conversational context but memory files persist. Always check `reference/launchmate/gotchas.md` and `reference/launchmate/conventions.md` for established positions before stating something that might contradict an earlier conversation.
- **Don't state things as fact if you lost the context for them**: If Laura references something from before a compaction that you can't verify, say "I lost that context in compaction" rather than guessing or contradicting yourself.
