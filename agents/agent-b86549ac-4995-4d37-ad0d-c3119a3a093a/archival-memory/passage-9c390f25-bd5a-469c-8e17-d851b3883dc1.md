---
id: passage-9c390f25-bd5a-469c-8e17-d851b3883dc1
created_at: 2026-07-15T21:52:22.135890Z
tags: [tidbits, github-actions, fix, infrastructure, memory-sync, july-2026]
---

July 15, 2026 — Infrastructure Fixes: Tidbits Dashboard Workflow + Memory Sync

## Tidbits Dashboard Workflow Fix
- **Issue:** GitHub Actions "Update Dashboard" workflow in golaunchmate/tidbits repo consistently failing (runs #210-216)
- **Root Cause 1:** `dashboard/update-dashboard.ps1` had a corrupted line 8 (broken string literal with comment text fused in) AND hardcoded local path `C:\Users\marga\OneDrive\Documents\GitHub\golaunchmate\tidbits` that doesn't exist on GitHub Actions runners
- **Fix Applied:** Replaced hardcoded path with `$PSScriptRoot` dynamic resolution (falls back to `$GITHUB_WORKSPACE`). Script now runs correctly on GitHub Actions — scanned 109 founders, 150 files, updated data.json successfully
- **Root Cause 2:** `git push` step fails with 403 — golaunchmate organization has "write permissions for workflows" disabled at the org level. The GITHUB_TOKEN can't push changes back to the repo
- **Remaining Action for Laura:** Either change org setting (GitHub → golaunchmate → Settings → Actions → General → Workflow permissions → "Read and write") OR create a PAT with `repo` scope and add as `PAT_TOKEN` secret in tidbits repo
- **Note:** Could not modify the workflow YAML directly because the GITHUB_TOKEN lacks `workflow` scope

## Memory Sync to GitHub
- **Issue:** Laura's main agent (agent-b86549ac) memory was not synced to GitHub. Existing sync scripts in memories repo were hardcoded to Letta Code agent (agent-8f31ed67)
- **Fix Applied:** Used Letta API to fetch all 31 memory blocks, synced 28 non-empty blocks to golaunchmate/memories repo under `agents/agent-b86549ac-4995-4d37-ad0d-c3119a3a093a/`
- **Created:** `agent.json` metadata file with block inventory
- **Created:** Reusable `sync-agent-memory.ps1` script that can sync ANY agent's memory via the Letta API (parameterized by agent ID)
- **Repo:** https://github.com/golaunchmate/memories/tree/main/agents/agent-b86549ac-4995-4d37-ad0d-c3119a3a093a